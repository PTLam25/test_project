import 'package:flutter/material.dart';

class PasswordInputPage extends StatelessWidget {
  const PasswordInputPage({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('PasswordInputPage'),
      ),
    );
  }
}
