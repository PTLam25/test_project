import 'package:flutter/material.dart';

class SmsCodeConfirmationPage extends StatelessWidget {
  const SmsCodeConfirmationPage({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('SmsCodeConfirmationPage'),
      ),
    );
  }
}
